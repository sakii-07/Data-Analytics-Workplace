-- mysql shell commands :-

--     1) \sql
--     2)\connect root@127.0.0.1

-- SQL commands :- 

-- 1) show databases;
-- 2) create database db_1;  or create schema database_name;
-- 3) use db_1;
-- 4) show tables;

-- DDL commands :-
--     1) create table table1(col1 int, col2 int);  ---- (1)
--     2) drop table table1;
--     3) truncate table table1;

-- DML commands :-
--     1) insert into t_name values (v1, v2, ....);
--     2) insert into t_name (col1,cl2, ...) values (v1, v2, ...);

-- DQL command :-
--     1) select * from t_name;
--     2) desc table1;  or  describe table1;


-- Q. create a table with name student marks, columns like rollno name tast1 test2 test3 and 5 records